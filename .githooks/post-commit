#!/bin/sh
# После каждого локального commit — push в текущую ветку (нужен настроенный remote).
git rev-parse --git-dir >/dev/null 2>&1 || exit 0
branch=$(git rev-parse --abbrev-ref HEAD)
[ "$branch" = "HEAD" ] && exit 0
git push origin "$branch" 2>/dev/null || git push 2>/dev/null || true
